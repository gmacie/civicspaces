<%= link_to "Civicspaces", root_path, :class => 'brand' %>
<ul class="nav">
	<% if user_signed_in? %>
	  <li>
	    <%= link_to 'logout', destroy_user_sessio_path, :method => 'delete' %>
	  </li>
	<% else %>
	  <li>
	    <%= link_to 'login', new_user_session_path %>
	  </li>
	<% end %>
  <% if user_signed_in? %>
    <li>
    	<%= link_to 'Edit account', edit_user_registration_path %>
    </li>
  <% else %>
    <li>
    	<%= link_to 'Sign up', new_user_registration_path %>
    </li>
  <% end %>
</ul>

